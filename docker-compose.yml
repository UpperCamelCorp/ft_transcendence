services:
  app:
    platform: linux/amd64
    build: .
    container_name: ft_transcendence
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/public/dist
    restart: unless-stopped
